# frontend/

The localhost settings & overview UI - **separate from the backend**. No Python
application logic lives here; it consumes the `src/mika/web/` API.

- `templates/` - server-rendered pages (Jinja2).
- `static/` - css, js, images.
