# deploy/

Deployment artifacts for a VPS install - templates and configs, not application
code.

- `systemd/` - unit + timer templates the installer renders.
- `docker/` - optional container build / compose.
