# ai/llm/tools/

Tool / function-calling support for a chat turn.

- `registry.py` - the registry of callable tools the chat model may invoke. Distinct from the reviewer tools in `ai/learning/reviewer/tools/`.
