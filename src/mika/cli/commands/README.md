# cli/commands/

One file per CLI command. `run`/`web` launch subsystems, `setup` is the wizard,
`service` controls `system/`, and `relationship_memory` provides bounded archive
backfill plus content-free inspection and derived-memory deletion.
