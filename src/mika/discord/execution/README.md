# Discord execution

This package is the sole adapter that turns a platform-neutral action plan into Discord API
calls. It reports successful visible actions and typed failures; it contains no participation,
generation, or memory policy.
